<div class="_contract">
  質問タイトル：<%= guest_question_contract.contractable.title %></br>
  質問投稿者：<%= guest_question_contract.contractable.user.name %></br>
  <%= link_to 'この質問を評価する', new_contract_reviews_path(guest_question_contract) %>
  <%= link_to "チャット", contract_chats_path(guest_question_contract.id) %></br>
</div>
